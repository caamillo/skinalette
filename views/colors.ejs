<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="./css/tailwindcompiled.css">
    <title>Document</title>
</head>
<body>
    <div id="colors" class="flex items-center justify-center"></div>
    <script>
        var colors = "<%= locals.colors %>";
        colors = JSON.parse(atob(colors))
        console.log(colors)
        const colorsDiv = document.getElementById('colors')
        for(let color of colors){
            const colorDiv = document.createElement('div')
            colorDiv.style.cssText = 'width: 100px; height: 100px; background-color: ' + color
            colorsDiv.appendChild(colorDiv)
        }
    </script>
</body>
</html>